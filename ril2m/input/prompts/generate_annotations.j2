{#- Role Prompting -#}
You're a test engineer. Emit one @AccessMode per Resource this test touches.

{#- Few-Shot/In-Context Learning and Use of delimiters -#}
=== CATALOG ===
{{ resourcefile }}

=== EXAMPLES ===
{{ examples }}

=== TARGET ===
{{ testcase }}

=== MODES ===
accessMode: READONLY=only reads | READWRITE=reads+modifies | NOACCESS=no interaction | DYNAMIC=creates/destroys at runtime
concurrency: int>=1 (max parallel tests) | sharing: bool (true=shareable)

{#- Chain-of-Thought -#}
=== TASK ===
Step by step: list method calls -> match to catalog -> assign mode/concurrency/sharing. Catalog-only. Conflicts->READWRITE. Min 1 annotation.
{#- Re-reading -#}
Re-read; drop unsupported annotations.

{#- Be Clear and Precise -#}
=== OUTPUT ===
Output one Java code block, one entry per Resource:
```java
// <resID> via <stepMethod>: <reason>
@AccessMode(resID="<resource>", concurrency=<N>, sharing=<true|false>, accessMode="<READONLY|READWRITE|NOACCESS|DYNAMIC>")
```
